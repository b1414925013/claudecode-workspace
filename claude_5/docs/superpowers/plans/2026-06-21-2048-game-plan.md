# 2048 Game Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a fully playable 2048 web game with Vue 3 + Vite + TypeScript, supporting multiple board sizes, dark mode, undo, and persistence.

**Architecture:** Single-page Vue 3 app using Composition API. Game logic isolated in composables (`useGameLogic`, `useGameState`, `useStorage`). UI rendered with 5 scoped-CSS components. Tiles positioned absolutely with CSS transforms for smooth animation.

**Tech Stack:** Vue 3.4, Vite 5, TypeScript 5.4, CSS Custom Properties, localStorage

## Global Constraints

- Vue 3 Composition API + `<script setup lang="ts">` throughout
- Zero external UI/framework dependencies (no Tailwind, no UI library)
- Build must pass with `vue-tsc --noEmit && vite build`
- All component styles scoped unless global (style.css)
- Files use kebab-case naming: `GameHeader.vue`, `useGameState.ts`
- Tile animations use CSS `transition` on `transform` only (no JS animation libs)
- localStorage read/write wrapped in try/catch for privacy-mode fallback

---

### Task 1: Scaffold Vite + Vue 3 + TypeScript Project

**Files:**
- Create: `2048-game/package.json`
- Create: `2048-game/vite.config.ts`
- Create: `2048-game/tsconfig.json`
- Create: `2048-game/tsconfig.node.json`
- Create: `2048-game/index.html`
- Create: `2048-game/src/vite-env.d.ts`
- Create: `2048-game/src/main.ts`

- [ ] **Step 1: Create package.json**

```json
{
  "name": "2048-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vue-tsc": "^2.0.0"
  }
}
```

- [ ] **Step 2: Create vite.config.ts**

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

- [ ] **Step 3: Create tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"]
}
```

- [ ] **Step 4: Create tsconfig.node.json**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 5: Create index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
    <title>2048</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🎮</text></svg>" />
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 6: Create src/vite-env.d.ts**

```typescript
/// <reference types="vite/client" />
declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

- [ ] **Step 7: Create src/main.ts**

```typescript
import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

createApp(App).mount('#app')
```

- [ ] **Step 8: Create stub App.vue**

```vue
<template>
  <div class="app">
    <h1>2048</h1>
  </div>
</template>

<script setup lang="ts">
</script>
```

- [ ] **Step 9: Install dependencies**

Run:
```bash
cd 2048-game && npm install
```

Expected: node_modules created, no errors.

- [ ] **Step 10: Verify build**

Run:
```bash
npm run build
```

Expected: `dist/` folder created with index.html + JS bundle.

- [ ] **Step 11: Commit**

```bash
git add 2048-game/
git commit -m "feat: scaffold Vite + Vue 3 + TypeScript 2048 project"
```

---

### Task 2: Types and Storage Layer

**Files:**
- Create: `2048-game/src/types/game.ts`
- Create: `2048-game/src/composables/useStorage.ts`

**Interfaces:**
- Produces: `TileData`, `BoardSize`, `Direction`, `GameStatus`, `SerializedTile`, `StorageData` types
- Produces: `useStorage()` with `saveState()`, `loadState()`, `saveBestScore()`, `loadBestScore()`, `saveDarkMode()`, `loadDarkMode()`

- [ ] **Step 1: Create src/types/game.ts**

```typescript
export interface TileData {
  id: number
  value: number
  row: number
  col: number
  isNew?: boolean    // true for tiles just spawned (pop-in animation)
  isMerged?: boolean // true for tiles just merged (pulse animation)
}

export type BoardSize = 4 | 5 | 6
export type Direction = 'up' | 'down' | 'left' | 'right'
export type GameStatus = 'playing' | 'won' | 'lost'

export interface SerializedTile {
  id: number
  value: number
  row: number
  col: number
}

export interface StorageData {
  tiles: SerializedTile[]
  score: number
  boardSize: BoardSize
}

export interface GameState {
  tiles: TileData[]
  score: number
  bestScore: Record<BoardSize, number>
  status: GameStatus
  boardSize: BoardSize
  history: { tiles: TileData[]; score: number }[]
  isDarkMode: boolean
}
```

- [ ] **Step 2: Create src/composables/useStorage.ts**

```typescript
import type { BoardSize, GameState, StorageData } from '../types/game'

const STATE_KEY = '2048-game-state'
const BEST_KEY = '2048-best-score'
const DARK_KEY = '2048-dark-mode'

export function useStorage() {
  function isAvailable(): boolean {
    try {
      const k = '__test__'
      localStorage.setItem(k, '1')
      localStorage.removeItem(k)
      return true
    } catch {
      return false
    }
  }

  const available = isAvailable()

  function saveState(state: Pick<GameState, 'tiles' | 'score' | 'boardSize'>): void {
    if (!available) return
    try {
      const data: StorageData = {
        tiles: state.tiles.map(t => ({ id: t.id, value: t.value, row: t.row, col: t.col })),
        score: state.score,
        boardSize: state.boardSize,
      }
      localStorage.setItem(STATE_KEY, JSON.stringify(data))
    } catch { /* silent */ }
  }

  function loadState(): StorageData | null {
    if (!available) return null
    try {
      const raw = localStorage.getItem(STATE_KEY)
      if (!raw) return null
      const parsed = JSON.parse(raw) as StorageData
      // Validate basic shape
      if (!parsed.tiles || typeof parsed.score !== 'number') {
        localStorage.removeItem(STATE_KEY)
        return null
      }
      return parsed
    } catch {
      localStorage.removeItem(STATE_KEY)
      return null
    }
  }

  function clearState(): void {
    if (!available) return
    try { localStorage.removeItem(STATE_KEY) } catch { /* silent */ }
  }

  function saveBestScore(size: BoardSize, score: number): void {
    if (!available) return
    try {
      const scores = loadBestScore()
      scores[size] = Math.max(scores[size] || 0, score)
      localStorage.setItem(BEST_KEY, JSON.stringify(scores))
    } catch { /* silent */ }
  }

  function loadBestScore(): Record<BoardSize, number> {
    if (!available) return { 4: 0, 5: 0, 6: 0 }
    try {
      const raw = localStorage.getItem(BEST_KEY)
      return raw ? { 4: 0, 5: 0, 6: 0, ...JSON.parse(raw) } : { 4: 0, 5: 0, 6: 0 }
    } catch {
      return { 4: 0, 5: 0, 6: 0 }
    }
  }

  function saveDarkMode(isDark: boolean): void {
    if (!available) return
    try { localStorage.setItem(DARK_KEY, JSON.stringify(isDark)) } catch { /* silent */ }
  }

  function loadDarkMode(): boolean | null {
    if (!available) return null
    try {
      const raw = localStorage.getItem(DARK_KEY)
      return raw ? JSON.parse(raw) : null
    } catch {
      return null
    }
  }

  return { saveState, loadState, clearState, saveBestScore, loadBestScore, saveDarkMode, loadDarkMode }
}
```

- [ ] **Step 3: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 4: Commit**

```bash
git add 2048-game/src/types/ 2048-game/src/composables/
git commit -m "feat: add types and localStorage composable"
```

---

### Task 3: Core Game Logic Composable

**Files:**
- Create: `2048-game/src/composables/useGameLogic.ts`

**Interfaces:**
- Consumes: `TileData`, `BoardSize`, `Direction` from `types/game.ts`
- Produces: `useGameLogic()` returning `createInitialTiles()`, `executeMove()`, `checkWin()`, `checkLose()`, `canMove()`, `generateTile()`, `resetIdCounter()`

- [ ] **Step 1: Create src/composables/useGameLogic.ts**

```typescript
import type { TileData, BoardSize, Direction } from '../types/game'

interface MoveResult {
  tiles: TileData[]
  score: number
  moved: boolean
}

interface SlideRowResult {
  result: (TileData | null)[]
  score: number
  moved: boolean
}

export function useGameLogic() {
  let nextTileId = 1

  function generateId(): number {
    return nextTileId++
  }

  function resetIdCounter(maxId?: number): void {
    nextTileId = (maxId || 0) + 1
  }

  function getNextId(): number {
    return nextTileId
  }

  function shuffle<T>(arr: T[]): T[] {
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]]
    }
    return arr
  }

  function createInitialTiles(size: BoardSize): TileData[] {
    const positions: [number, number][] = []
    for (let r = 0; r < size; r++) {
      for (let c = 0; c < size; c++) {
        positions.push([r, c])
      }
    }
    shuffle(positions)

    const tiles: TileData[] = []
    for (let i = 0; i < 2; i++) {
      tiles.push({
        id: generateId(),
        value: Math.random() < 0.9 ? 2 : 4,
        row: positions[i][0],
        col: positions[i][1],
        isNew: true,
      })
    }
    return tiles
  }

  /**
   * Slide and merge a single line (array of tiles with possible null gaps).
   * Processes toward index 0 ("left").
   * Returns the result array and any score gained.
   */
  function slideRow(line: (TileData | null)[], size: number): SlideRowResult {
    // Extract non-null tiles
    const tiles = line.filter((t): t is TileData => t !== null)
    if (tiles.length === 0) {
      return { result: Array(size).fill(null), score: 0, moved: false }
    }

    const result: (TileData | null)[] = Array(size).fill(null)
    let score = 0
    let outputIdx = 0
    let i = 0

    while (i < tiles.length) {
      if (i + 1 < tiles.length && tiles[i].value === tiles[i + 1].value) {
        // Merge: keep the first tile's id, consume the second
        result[outputIdx] = {
          id: tiles[i].id,
          value: tiles[i].value * 2,
          row: tiles[i].row,
          col: outputIdx,
          isMerged: true,
        }
        score += tiles[i].value * 2
        i += 2
      } else {
        result[outputIdx] = {
          ...tiles[i],
          col: outputIdx,
          isNew: false,
          isMerged: false,
        }
        i++
      }
      outputIdx++
    }

    // Determine if anything moved by comparing original positions to output
    let moved = false
    for (let j = 0; j < size; j++) {
      const orig = line[j]
      const next = result[j]
      if (orig === null && next !== null) moved = true
      else if (orig !== null && next === null) moved = true
      else if (orig !== null && next !== null && orig.id !== next.id) moved = true
    }

    return { result, score, moved }
  }

  function executeMove(tiles: TileData[], direction: Direction, size: BoardSize): MoveResult {
    let totalScore = 0
    let anyMoved = false
    const newTiles: TileData[] = []

    for (let i = 0; i < size; i++) {
      // Extract one line based on direction
      const line: (TileData | null)[] = Array(size).fill(null)

      for (let j = 0; j < size; j++) {
        const { r, c } = dirToCoords(direction, i, j, size)
        const tile = tiles.find(t => t.row === r && t.col === c)
        line[j] = tile || null
      }

      // Slide the line
      const { result, score, moved } = slideRow(line, size)
      totalScore += score
      if (moved) anyMoved = true

      // Map result back to tile positions
      for (let j = 0; j < size; j++) {
        const tile = result[j]
        if (tile) {
          const { r, c } = dirToCoords(direction, i, j, size)
          newTiles.push({ ...tile, row: r, col: c })
        }
      }
    }

    return { tiles: newTiles, score: totalScore, moved: anyMoved }
  }

  /**
   * Translate (lineIndex, positionInLine, direction) → (row, col) on the board.
   * For 'left':  lineIndex = row, positionInLine = col
   * For 'right': lineIndex = row, positionInLine = (size-1-col)
   * For 'up':    lineIndex = col, positionInLine = row
   * For 'down':  lineIndex = col, positionInLine = (size-1-row)
   */
  function dirToCoords(dir: Direction, lineIdx: number, pos: number, size: number): { r: number; c: number } {
    switch (dir) {
      case 'left':  return { r: lineIdx, c: pos }
      case 'right': return { r: lineIdx, c: size - 1 - pos }
      case 'up':    return { r: pos, c: lineIdx }
      case 'down':  return { r: size - 1 - pos, c: lineIdx }
    }
  }

  function generateTile(tiles: TileData[], size: BoardSize): TileData {
    const occupied = new Set(tiles.map(t => `${t.row},${t.col}`))
    const empty: [number, number][] = []
    for (let r = 0; r < size; r++) {
      for (let c = 0; c < size; c++) {
        if (!occupied.has(`${r},${c}`)) empty.push([r, c])
      }
    }
    const [row, col] = empty[Math.floor(Math.random() * empty.length)]
    return {
      id: generateId(),
      value: Math.random() < 0.9 ? 2 : 4,
      row,
      col,
      isNew: true,
    }
  }

  function checkWin(tiles: TileData[]): boolean {
    return tiles.some(t => t.value >= 2048)
  }

  function checkLose(tiles: TileData[], size: BoardSize): boolean {
    // Lose condition: no empty cells AND no adjacent equal values
    if (tiles.length < size * size) return false

    for (let r = 0; r < size; r++) {
      for (let c = 0; c < size; c++) {
        const tile = tiles.find(t => t.row === r && t.col === c)
        if (!tile) return false

        const right = tiles.find(t => t.row === r && t.col === c + 1)
        if (right && right.value === tile.value) return false

        const down = tiles.find(t => t.row === r + 1 && t.col === c)
        if (down && down.value === tile.value) return false
      }
    }

    return true
  }

  function canMove(tiles: TileData[], size: BoardSize): boolean {
    if (tiles.length < size * size) return true
    return !checkLose(tiles, size)
  }

  return {
    createInitialTiles,
    executeMove,
    generateTile,
    checkWin,
    checkLose,
    canMove,
    resetIdCounter,
    getNextId,
    shuffle,
  }
}
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/composables/useGameLogic.ts
git commit -m "feat: add core 2048 game logic composable"
```

---

### Task 4: Game State Composable

**Files:**
- Create: `2048-game/src/composables/useGameState.ts`

**Interfaces:**
- Consumes: `GameState`, `TileData`, `BoardSize`, `Direction` from types; `useGameLogic()`; `useStorage()`
- Produces: `useGameState()` returning reactive `state` and `initGame()`, `handleMove()`, `undo()`, `continueAfterWin()`, `toggleDarkMode()`

- [ ] **Step 1: Create src/composables/useGameState.ts**

```typescript
import { reactive } from 'vue'
import type { GameState, Direction } from '../types/game'
import { useGameLogic } from './useGameLogic'
import { useStorage } from './useStorage'

export function useGameState() {
  const logic = useGameLogic()
  const storage = useStorage()

  // Determine initial dark mode
  const savedDark = storage.loadDarkMode()
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
  const initialDark = savedDark !== null ? savedDark : prefersDark
  if (initialDark) document.documentElement.classList.add('dark')

  const state = reactive<GameState>({
    tiles: [],
    score: 0,
    bestScore: storage.loadBestScore(),
    status: 'playing',
    boardSize: 4,
    history: [],
    isDarkMode: initialDark,
  })

  let animationLocked = false

  function initGame(size: number = state.boardSize): void {
    const boardSize = size as 4 | 5 | 6
    state.tiles = logic.createInitialTiles(boardSize)
    state.score = 0
    state.boardSize = boardSize
    state.status = 'playing'
    state.history = []
    animationLocked = false
    storage.saveState({ tiles: state.tiles, score: state.score, boardSize: state.boardSize })
  }

  function handleMove(direction: Direction): void {
    if (state.status !== 'playing') return
    if (animationLocked) return

    // Save snapshot for undo
    state.history.push({
      tiles: state.tiles.map(t => ({ ...t, isNew: false, isMerged: false })),
      score: state.score,
    })
    if (state.history.length > 10) state.history.shift()

    const { tiles: newTiles, score: addScore, moved } = logic.executeMove(state.tiles, direction, state.boardSize)

    if (!moved) {
      state.history.pop() // undo wasted history entry
      return
    }

    state.score += addScore
    state.tiles = newTiles

    // Spawn a new tile
    const spawnTile = logic.generateTile(state.tiles, state.boardSize)
    state.tiles.push(spawnTile)

    // Lock input during animation
    animationLocked = true
    setTimeout(() => { animationLocked = false }, 150)

    // Clear animation flags after animation completes
    setTimeout(() => {
      state.tiles.forEach(t => { t.isNew = false; t.isMerged = false })
    }, 200)

    // Auto-save
    storage.saveState({ tiles: state.tiles, score: state.score, boardSize: state.boardSize })

    // Update best score
    if (state.score > (state.bestScore[state.boardSize] || 0)) {
      state.bestScore[state.boardSize] = state.score
      storage.saveBestScore(state.boardSize, state.score)
    }

    // Check win / lose
    if (logic.checkWin(state.tiles)) {
      state.status = 'won'
    } else if (logic.checkLose(state.tiles, state.boardSize)) {
      state.status = 'lost'
    }
  }

  function undo(): void {
    if (state.history.length === 0) return
    const last = state.history.pop()!
    state.tiles = last.tiles.map(t => ({ ...t, isNew: false, isMerged: false }))
    state.score = last.score
    state.status = 'playing'
    storage.saveState({ tiles: state.tiles, score: state.score, boardSize: state.boardSize })
  }

  function continueAfterWin(): void {
    state.status = 'playing'
  }

  function toggleDarkMode(): void {
    state.isDarkMode = !state.isDarkMode
    document.documentElement.classList.toggle('dark', state.isDarkMode)
    storage.saveDarkMode(state.isDarkMode)
  }

  function newGame(size?: number): void {
    const s = size || state.boardSize
    if (s !== state.boardSize) {
      state.bestScore = storage.loadBestScore()
    }
    initGame(s)
  }

  return {
    state,
    initGame,
    handleMove,
    undo,
    continueAfterWin,
    toggleDarkMode,
    newGame,
  }
}
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/composables/useGameState.ts
git commit -m "feat: add game state composable with undo, persistence"
```

---

### Task 5: Global CSS Styles

**Files:**
- Create: `2048-game/src/style.css`

- [ ] **Step 1: Create src/style.css**

```css
/* ========== CSS Variables & Theme ========== */
:root {
  --board-bg: #bbada0;
  --cell-bg: rgba(238, 228, 218, 0.35);
  --page-bg: #faf8ef;
  --text-color: #776e65;
  --text-light: #f9f6f2;
  --score-bg: #bbada0;
  --btn-bg: #8f7a66;
  --btn-hover: #9f8b78;
  --overlay-bg: rgba(238, 228, 218, 0.73);
  --gap: 8px;
  --radius: 6px;
  --max-width: 500px;
}

.dark {
  --board-bg: #3c3a3d;
  --cell-bg: rgba(255, 255, 255, 0.1);
  --page-bg: #1a1a2e;
  --text-color: #e0e0e0;
  --score-bg: #3c3a3d;
  --btn-bg: #5c5a5d;
  --btn-hover: #6c6a6d;
  --overlay-bg: rgba(26, 26, 46, 0.85);
}

/* ========== Reset ========== */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background: var(--page-bg);
  color: var(--text-color);
  overflow-x: hidden;
  -webkit-tap-highlight-color: transparent;
  user-select: none;
}

#app {
  min-height: 100%;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding: 20px 16px 40px;
}

/* ========== Tile Colors ========== */
.tile-2    { background: #eee4da; color: #776e65; }
.tile-4    { background: #ede0c8; color: #776e65; }
.tile-8    { background: #f2b179; color: #f9f6f2; }
.tile-16   { background: #f59563; color: #f9f6f2; }
.tile-32   { background: #f67c5f; color: #f9f6f2; }
.tile-64   { background: #f65e3b; color: #f9f6f2; }
.tile-128  { background: #edcf72; color: #f9f6f2; font-size: 0.9em; }
.tile-256  { background: #edcc61; color: #f9f6f2; font-size: 0.9em; }
.tile-512  { background: #edc850; color: #f9f6f2; font-size: 0.9em; }
.tile-1024 { background: #edc53f; color: #f9f6f2; font-size: 0.75em; }
.tile-2048 { background: #edc22e; color: #f9f6f2; font-size: 0.75em; }
.tile-super { background: #3c3a32; color: #f9f6f2; font-size: 0.65em; }

.dark .tile-2    { background: #4a4a4a; color: #e0e0e0; }
.dark .tile-4    { background: #5a4a3a; color: #e0e0e0; }
.dark .tile-8    { background: #8a5a3a; color: #e0e0e0; }
.dark .tile-16   { background: #9a4a2a; color: #e0e0e0; }
.dark .tile-32   { background: #aa3a2a; color: #e0e0e0; }
.dark .tile-64   { background: #ba2a1a; color: #e0e0e0; }
.dark .tile-128  { background: #8a7a2a; color: #e0e0e0; }
.dark .tile-256  { background: #9a8a2a; color: #e0e0e0; }
.dark .tile-512  { background: #aa9a2a; color: #e0e0e0; }
.dark .tile-1024 { background: #baaa2a; color: #e0e0e0; }
.dark .tile-2048 { background: #ccaa1a; color: #e0e0e0; }
.dark .tile-super { background: #5a5a5a; color: #e0e0e0; }
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/style.css
git commit -m "feat: add global styles with tile colors and dark mode CSS variables"
```

---

### Task 6: GameTile Component

**Files:**
- Create: `2048-game/src/components/GameTile.vue`

**Interfaces:**
- Props: `tile: TileData`, `cellSize: number`, `gap: number`
- Emits: none (pure display)
- Produces animated tile element with CSS transition on transform

- [ ] **Step 1: Create src/components/GameTile.vue**

```vue
<template>
  <div
    class="tile"
    :class="[tileClass, { 'tile--new': tile.isNew, 'tile--merged': tile.isMerged }]"
    :style="tilePosition"
  >
    {{ tile.value }}
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { TileData } from '../types/game'

const props = defineProps<{
  tile: TileData
  cellSize: number
  gap: number
}>()

const tileClass = computed(() => {
  if (tile.value >= 8192) return 'tile-super'
  return `tile-${tile.value}`
})

const tilePosition = computed(() => ({
  transform: `translate(${props.tile.col * (props.cellSize + props.gap) + props.gap}px, ${props.tile.row * (props.cellSize + props.gap) + props.gap}px)`,
  width: `${props.cellSize}px`,
  height: `${props.cellSize}px`,
  lineHeight: `${props.cellSize}px`,
  fontSize: props.cellSize > 60 ? (tileClass.value === 'tile-1024' || tileClass.value === 'tile-2048' ? '1.8rem' : '2.5rem') : (tileClass.value === 'tile-1024' || tileClass.value === 'tile-2048' ? '1.2rem' : '1.8rem'),
}))
</script>

<style scoped>
.tile {
  position: absolute;
  top: 0;
  left: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  font-weight: bold;
  border-radius: 4px;
  text-align: center;
  z-index: 10;
  transition: transform 100ms ease;
  will-change: transform;
  /* Ensure tile text is centered */
  line-height: 1 !important;
}

.tile--new {
  animation: tile-pop-in 200ms ease-out;
}

.tile--merged {
  animation: tile-pulse 200ms ease-in-out;
}

@keyframes tile-pop-in {
  0%   { transform: scale(0); opacity: 0; }
  50%  { transform: scale(1.1); }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes tile-pulse {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.15); }
  100% { transform: scale(1); }
}

/* Fix: merged tiles also need their translate, animation handles scale overlay */
.tile--merged {
  animation: tile-pulse 200ms ease-in-out;
}
</style>
```

Note: For `tile--new` and `tile--merged` animations, Vue applies the inline `transform` for positioning and the CSS `animation` adds a scale overlay. CSS animations don't override inline `transform` — they run on the `animation` composite layer — so the translate is preserved.

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameTile.vue
git commit -m "feat: add GameTile component with pop-in and pulse animations"
```

---

### Task 7: GameBoard Component

**Files:**
- Create: `2048-game/src/components/GameBoard.vue`

**Interfaces:**
- Consumes: `TileData`, `BoardSize`, `Direction` types
- Props: `tiles: TileData[]`, `boardSize: BoardSize`, `onMove: (dir: Direction) => void`
- Renders grid background + absolutely-positioned tiles
- Handles keyboard arrow keys + touch swipe

- [ ] **Step 1: Create src/components/GameBoard.vue**

```vue
<template>
  <div
    ref="boardRef"
    class="board"
    :style="gridStyle"
    @keydown.prevent="onKeyDown"
    @touchstart.prevent="onTouchStart"
    @touchmove.prevent="onTouchMove"
    @touchend.prevent="onTouchEnd"
    tabindex="0"
    role="grid"
    aria-label="2048 游戏棋盘"
  >
    <!-- Background cells -->
    <div
      v-for="i in boardSize * boardSize"
      :key="'cell-' + i"
      class="cell"
    ></div>

    <!-- Tiles layer -->
    <GameTile
      v-for="tile in tiles"
      :key="tile.id"
      :tile="tile"
      :cell-size="cellSize"
      :gap="gap"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onBeforeUnmount } from 'vue'
import type { BoardSize, Direction, TileData } from '../types/game'
import GameTile from './GameTile.vue'

const props = defineProps<{
  tiles: TileData[]
  boardSize: BoardSize
}>()

const emit = defineEmits<{
  move: [direction: Direction]
}>()

const gap = 8
const boardRef = ref<HTMLDivElement | null>(null)
const boardWidth = ref(500)

const cellSize = computed(() => {
  return (boardWidth.value - gap * (props.boardSize + 1)) / props.boardSize
})

const gridStyle = computed(() => ({
  width: `${boardWidth.value}px`,
  height: `${boardWidth.value}px`,
  gridTemplateColumns: `repeat(${props.boardSize}, ${cellSize.value}px)`,
  gridTemplateRows: `repeat(${props.boardSize}, ${cellSize.value}px)`,
  gap: `${gap}px`,
  padding: `${gap}px`,
}))

function updateBoardWidth() {
  if (boardRef.value) {
    const parent = boardRef.value.parentElement
    if (parent) {
      const maxW = Math.min(parent.clientWidth - 32, 500)
      boardWidth.value = Math.max(maxW, 280)
    }
  }
}

// --- Keyboard ---
function onKeyDown(e: KeyboardEvent) {
  const dirMap: Record<string, Direction> = {
    ArrowUp: 'up', ArrowDown: 'down', ArrowLeft: 'left', ArrowRight: 'right',
  }
  const dir = dirMap[e.key]
  if (dir) {
    e.preventDefault()
    emit('move', dir)
  }
}

// --- Touch ---
let touchStartX = 0
let touchStartY = 0
const MIN_SWIPE = 20

function onTouchStart(e: TouchEvent) {
  const t = e.touches[0]
  touchStartX = t.clientX
  touchStartY = t.clientY
}

function onTouchMove(_e: TouchEvent) {
  // Prevent scroll while swiping on the board
}

function onTouchEnd(e: TouchEvent) {
  const t = e.changedTouches[0]
  const dx = t.clientX - touchStartX
  const dy = t.clientY - touchStartY
  const absDx = Math.abs(dx)
  const absDy = Math.abs(dy)

  if (Math.max(absDx, absDy) < MIN_SWIPE) return

  if (absDx > absDy) {
    emit('move', dx > 0 ? 'right' : 'left')
  } else {
    emit('move', dy > 0 ? 'down' : 'up')
  }
}

// --- Resize ---
let resizeObserver: ResizeObserver | null = null

onMounted(() => {
  updateBoardWidth()
  boardRef.value?.focus()
  resizeObserver = new ResizeObserver(updateBoardWidth)
  if (boardRef.value?.parentElement) {
    resizeObserver.observe(boardRef.value.parentElement)
  }
})

onBeforeUnmount(() => {
  resizeObserver?.disconnect()
})
</script>

<style scoped>
.board {
  position: relative;
  background: var(--board-bg);
  border-radius: var(--radius);
  display: grid;
  outline: none;
  touch-action: none;
}

.cell {
  background: var(--cell-bg);
  border-radius: 4px;
}
</style>
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameBoard.vue
git commit -m "feat: add GameBoard with grid, keyboard, and touch input"
```

---

### Task 8: GameHeader Component

**Files:**
- Create: `2048-game/src/components/GameHeader.vue`

**Interfaces:**
- Props: `score: number`, `bestScore: number`
- Emits: `newGame`
- Displays title + score boxes + "新游戏" button

- [ ] **Step 1: Create src/components/GameHeader.vue**

```vue
<template>
  <div class="header">
    <div class="header-top">
      <h1 class="title">2048</h1>
      <div class="scores">
        <div class="score-box">
          <span class="score-label">分数</span>
          <span class="score-value" :key="score">{{ score }}</span>
        </div>
        <div class="score-box">
          <span class="score-label">最高分</span>
          <span class="score-value">{{ bestScore }}</span>
        </div>
      </div>
    </div>
    <div class="header-bottom">
      <p class="subtitle">合并数字，达到 <strong>2048</strong>！</p>
      <button class="btn" @click="$emit('newGame')">新游戏</button>
    </div>
  </div>
</template>

<script setup lang="ts">
defineProps<{
  score: number
  bestScore: number
}>()

defineEmits<{
  newGame: []
}>()
</script>

<style scoped>
.header {
  width: 100%;
  max-width: 500px;
  margin-bottom: 12px;
}

.header-top {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.title {
  font-size: 4rem;
  font-weight: 900;
  line-height: 1;
  color: var(--text-color);
}

.scores {
  display: flex;
  gap: 8px;
}

.score-box {
  background: var(--score-bg);
  color: #fff;
  border-radius: var(--radius);
  padding: 8px 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 80px;
}

.score-label {
  font-size: 0.7rem;
  text-transform: uppercase;
  opacity: 0.8;
  letter-spacing: 0.5px;
}

.score-value {
  font-size: 1.5rem;
  font-weight: 700;
}

.header-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 8px;
}

.subtitle {
  font-size: 0.9rem;
  color: var(--text-color);
  opacity: 0.8;
}

.btn {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 10px 20px;
  font-size: 0.9rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.15s;
}

.btn:hover {
  background: var(--btn-hover);
}

@media (max-width: 500px) {
  .title { font-size: 3rem; }
  .score-box { padding: 6px 14px; min-width: 60px; }
  .score-value { font-size: 1.2rem; }
}
</style>
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameHeader.vue
git commit -m "feat: add GameHeader with score display and new game button"
```

---

### Task 9: GameControls Component

**Files:**
- Create: `2048-game/src/components/GameControls.vue`

**Interfaces:**
- Props: `canUndo: boolean`, `boardSize: number`
- Emits: `undo`, `changeSize: BoardSize`, `toggleDark`

- [ ] **Step 1: Create src/components/GameControls.vue**

```vue
<template>
  <div class="controls">
    <button class="btn" :disabled="!canUndo" @click="$emit('undo')">
      ↩ 撤销
    </button>

    <div class="control-group">
      <label class="control-label">棋盘</label>
      <select class="select" :value="boardSize" @change="onSizeChange">
        <option :value="4">4×4</option>
        <option :value="5">5×5</option>
        <option :value="6">6×6</option>
      </select>
    </div>

    <button class="btn btn-icon" @click="$emit('toggleDark')" :title="isDark ? '切换亮色模式' : '切换深色模式'">
      {{ isDark ? '☀️' : '🌙' }}
    </button>
  </div>
</template>

<script setup lang="ts">
import type { BoardSize } from '../types/game'

defineProps<{
  canUndo: boolean
  boardSize: BoardSize
  isDark: boolean
}>()

const emit = defineEmits<{
  undo: []
  changeSize: [size: BoardSize]
  toggleDark: []
}>()

function onSizeChange(e: Event) {
  const val = parseInt((e.target as HTMLSelectElement).value) as BoardSize
  emit('changeSize', val)
}
</script>

<style scoped>
.controls {
  width: 100%;
  max-width: 500px;
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 12px;
}

.btn {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 10px 16px;
  font-size: 0.85rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.15s;
  white-space: nowrap;
}

.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.btn:not(:disabled):hover {
  background: var(--btn-hover);
}

.btn-icon {
  padding: 10px 12px;
  font-size: 1.1rem;
  line-height: 1;
}

.control-group {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-left: auto;
}

.control-label {
  font-size: 0.8rem;
  color: var(--text-color);
  opacity: 0.8;
}

.select {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 9px 12px;
  font-size: 0.85rem;
  font-weight: 600;
  cursor: pointer;
  outline: none;
  appearance: auto;
}

.select:hover {
  background: var(--btn-hover);
}

@media (max-width: 500px) {
  .controls { flex-wrap: wrap; }
  .control-group { margin-left: 0; }
}
</style>
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameControls.vue
git commit -m "feat: add GameControls with undo, size selector, dark mode toggle"
```

---

### Task 10: GameOverlay Component

**Files:**
- Create: `2048-game/src/components/GameOverlay.vue`

**Interfaces:**
- Props: `status: GameStatus`, `score: number`
- Emits: `newGame`
- Shows win or lose overlay with semi-transparent backdrop

- [ ] **Step 1: Create src/components/GameOverlay.vue**

```vue
<template>
  <Transition name="overlay">
    <div v-if="status !== 'playing'" class="overlay" @click.self="$emit('newGame')">
      <div class="overlay-content">
        <h2 class="overlay-title" v-if="status === 'won'">🎉 恭喜你赢了！</h2>
        <h2 class="overlay-title lose" v-else>😞 游戏结束</h2>
        <p class="overlay-score">得分：<strong>{{ score }}</strong></p>
        <button class="btn" @click="$emit('newGame')">再来一局</button>
      </div>
    </div>
  </Transition>
</template>

<script setup lang="ts">
import type { GameStatus } from '../types/game'

defineProps<{
  status: GameStatus
  score: number
}>()

defineEmits<{
  newGame: []
}>()
</script>

<style scoped>
.overlay {
  position: absolute;
  inset: 0;
  background: var(--overlay-bg);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 100;
  border-radius: var(--radius);
  backdrop-filter: blur(2px);
}

.overlay-content {
  text-align: center;
  padding: 32px;
}

.overlay-title {
  font-size: 2rem;
  margin-bottom: 12px;
  color: var(--text-color);
}

.overlay-title.lose {
  color: var(--text-color);
}

.overlay-score {
  font-size: 1.1rem;
  margin-bottom: 24px;
  color: var(--text-color);
}

.btn {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 12px 32px;
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.15s;
}

.btn:hover {
  background: var(--btn-hover);
}

/* Transition */
.overlay-enter-active {
  transition: opacity 0.3s ease;
}
.overlay-leave-active {
  transition: opacity 0.2s ease;
}
.overlay-enter-from,
.overlay-leave-to {
  opacity: 0;
}
</style>
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameOverlay.vue
git commit -m "feat: add GameOverlay for win/lose states"
```

---

### Task 11: App.vue Final Integration

**Files:**
- Modify: `2048-game/src/App.vue` (replace stub)
- All composables and components now exist — wire them together.

- [ ] **Step 1: Replace src/App.vue with full integration**

```vue
<template>
  <div class="app-container">
    <GameHeader
      :score="state.score"
      :best-score="state.bestScore[state.boardSize] || 0"
      @new-game="handleNewGame"
    />

    <div class="board-wrapper">
      <GameBoard
        :tiles="state.tiles"
        :board-size="state.boardSize"
        @move="state.handleMove"
      />
      <GameOverlay
        :status="state.status"
        :score="state.score"
        @new-game="handleNewGame"
      />
    </div>

    <GameControls
      :can-undo="state.history.length > 0"
      :board-size="state.boardSize"
      :is-dark="state.isDarkMode"
      @undo="state.undo"
      @change-size="handleSizeChange"
      @toggle-dark="state.toggleDarkMode"
    />
  </div>
</template>

<script setup lang="ts">
import { onMounted } from 'vue'
import GameHeader from './components/GameHeader.vue'
import GameBoard from './components/GameBoard.vue'
import GameControls from './components/GameControls.vue'
import GameOverlay from './components/GameOverlay.vue'
import { useGameState } from './composables/useGameState'
import type { BoardSize } from './types/game'

const { state, initGame, handleNewGame: newGame } = useGameState()

// Wrap handleMove to pass it to GameBoard as a function reference
// GameBoard receives `onMove` which we alias
function handleNewGame() {
  newGame(state.boardSize)
}

function handleSizeChange(size: BoardSize) {
  newGame(size)
}

onMounted(() => {
  // Try to restore saved state, otherwise start fresh
  const saved = false // useStorage handles this inside useGameState
  initGame(4)
})
</script>

<style scoped>
.app-container {
  width: 100%;
  max-width: 500px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.board-wrapper {
  position: relative;
  width: 100%;
  display: flex;
  justify-content: center;
}
</style>
```

Wait, there's an issue. The `GameBoard` emits `move`, but I'm using `state.handleMove` directly which is a method on the state object. Let me fix this — I need to bridge the emit from GameBoard to the state method.

Let me rewrite App.vue properly:

```vue
<template>
  <div class="app-container">
    <GameHeader
      :score="state.score"
      :best-score="state.bestScore[state.boardSize] || 0"
      @new-game="handleNewGame"
    />

    <div class="board-wrapper">
      <GameBoard
        :tiles="state.tiles"
        :board-size="state.boardSize"
        @move="onMove"
      />
      <GameOverlay
        :status="state.status"
        :score="state.score"
        @new-game="handleNewGame"
      />
    </div>

    <GameControls
      :can-undo="state.history.length > 0"
      :board-size="state.boardSize"
      :is-dark="state.isDarkMode"
      @undo="state.undo()"
      @change-size="handleSizeChange"
      @toggle-dark="state.toggleDarkMode()"
    />
  </div>
</template>

<script setup lang="ts">
import { onMounted } from 'vue'
import GameHeader from './components/GameHeader.vue'
import GameBoard from './components/GameBoard.vue'
import GameControls from './components/GameControls.vue'
import GameOverlay from './components/GameOverlay.vue'
import { useGameState } from './composables/useGameState'
import type { BoardSize, Direction } from './types/game'

const { state, initGame, handleMove, newGame: newGameAction } = useGameState()

function onMove(direction: Direction) {
  handleMove(direction)
}

function handleNewGame() {
  newGameAction(state.boardSize)
}

function handleSizeChange(size: BoardSize) {
  newGameAction(size)
}

onMounted(() => {
  // Try restoring from storage — useGameState already loads best scores
  // but we need to restore the full game state.
  // For simplicity, always start fresh.
  initGame(4)
})
</script>

<style scoped>
.app-container {
  width: 100%;
  max-width: 500px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.board-wrapper {
  position: relative;
  width: 100%;
  display: flex;
  justify-content: center;
}
</style>
```

Hmm, but from the spec: the game should auto-save and auto-restore. Let me add state restoration in onMounted:

```typescript
import { useStorage } from './composables/useStorage'

const storage = useStorage()

onMounted(() => {
  const saved = storage.loadState()
  if (saved && saved.tiles.length > 0) {
    // Restore saved game
    initGame(saved.boardSize)
    // Actually, we need a "restore" method or we need useGameState to handle this
    // ...
  } else {
    initGame(4)
  }
})
```

This is getting complicated. Let me handle restore in useGameState instead. I'll add logic to `initGame` to optionally restore from storage. Or better, I'll modify the plan so that App.vue calls a `restoreOrInit()` method.

Actually, let me just keep it simple in App.vue and add a `restoreFromStorage` method to useGameState:

```typescript
// In useGameState.ts
function restoreOrCreate(): void {
  const saved = storage.loadState()
  if (saved && saved.tiles.length > 0) {
    state.tiles = saved.tiles.map(t => ({ ...t }))
    state.score = saved.score
    state.boardSize = saved.boardSize
    state.status = 'playing'
    state.history = []
    state.bestScore = storage.loadBestScore()
  } else {
    initGame(4)
  }
}
```

Let me add this to useGameState in Task 4 and use it in App.vue. I should also make sure the `restoreOrCreate` function is exported.

OK, let me re-think the interface and fix the plan accordingly.

Actually, let me be more careful. In the plan, Task 4 (useGameState) currently doesn't have a restore function. I need to either:
1. Add it to Task 4
2. Or handle it differently in App.vue

I'll add a `restoreOrInit` exported function to useGameState in Task 4, and use it in App.vue Task 11.

Let me update the useGameState in Task 4 to include this function. And update App.vue in Task 11 to use it.

Let me re-draft the relevant parts.

For Task 4 useGameState, add:
```typescript
function restoreOrInit(): void {
  const saved = storage.loadState()
  if (saved && saved.tiles.length > 0) {
    state.tiles = saved.tiles.map(t => ({
      ...t,
      isNew: false,
      isMerged: false,
    }))
    state.score = saved.score
    state.boardSize = saved.boardSize
    state.status = 'playing'
    state.history = []
    state.bestScore = storage.loadBestScore()
  } else {
    initGame(4)
  }
}
```

And export it from the return object.

For Task 11 App.vue:
```typescript
onMounted(() => {
  restoreOrInit()
})
```

OK, I think everything is consistent now. Let me write the complete plan.

Let me also re-check the plan against the writing-plans skill requirements:
- ✅ Header with feature name, goal, architecture, tech stack, global constraints
- ✅ Tasks with Files and Interfaces sections
- ✅ Steps with actual code
- ✅ Exact commands
- ✅ Commit steps
- Needs spec coverage check and placeholder scan after writing

Let me also check spec coverage:

Spec requires:
1. ✅ Multiple board sizes (4×4/5×5/6×6) — Task 3 (BoardSize type), Task 4 (initGame), Task 9 (controls)
2. ✅ Dark mode — Task 5 (CSS vars), Task 9 (toggle), Task 4 (logic)
3. ✅ Undo (up to 10 steps) — Task 4 (history in useGameState)
4. ✅ Victory/failure detection — Task 3 (checkWin/checkLose), Task 10 (overlay)
5. ✅ Game state save/restore — Task 2 (storage), Task 4 (restoreOrInit)
6. ✅ Keyboard controls — Task 7 (onKeyDown)
7. ✅ Touch controls — Task 7 (onTouchStart/End)
8. ✅ Animations — Task 6 (tile pop-in/pulse)
9. ✅ localStorage with fallback — Task 2 (try/catch in useStorage)
10. ✅ Responsive design — Task 5 (media queries), Task 7 (board resizing)

Looks complete. Let me write the plan now.

One more thing: the plan document should not contain placeholder text. Let me make sure every code block is complete.

Also, I need to make sure the `useGameState.ts` code block in the plan is complete and properly handles the `restoreOrInit` export.

Let me finalize the plan document now.<｜end▁of▁thinking｜>

<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="Write">
<｜｜DSML｜｜parameter name="content" string="true"># 2048 Game Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a fully playable 2048 web game with Vue 3 + Vite + TypeScript, supporting multiple board sizes, dark mode, undo, and persistence.

**Architecture:** Single-page Vue 3 app using Composition API. Game logic isolated in composables (`useGameLogic`, `useGameState`, `useStorage`). UI rendered with 5 scoped-CSS components. Tiles positioned absolutely with CSS transforms for smooth animation.

**Tech Stack:** Vue 3.4, Vite 5, TypeScript 5.4, CSS Custom Properties, localStorage

## Global Constraints

- Vue 3 Composition API + `<script setup lang="ts">` throughout
- Zero external UI/framework dependencies (no Tailwind, no UI library)
- Build must pass with `vue-tsc --noEmit && vite build`
- All component styles scoped unless in global style.css
- Files use PascalCase for `.vue` components, camelCase for `.ts` composables
- Tile animations use CSS `transition` on `transform` only (no JS animation libs)
- localStorage read/write wrapped in try/catch for privacy-mode fallback
- Board input locked for 150ms after each move to prevent race conditions

---

### Task 1: Scaffold Vite + Vue 3 + TypeScript Project

**Files:**
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\package.json`
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\vite.config.ts`
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\tsconfig.json`
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\tsconfig.node.json`
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\index.html`
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\src\vite-env.d.ts`
- Create: `D:\develop\claudecode-workspace\claude_5\2048-game\src\main.ts`

- [ ] **Step 1: Create project directories**

```bash
mkdir -p 2048-game/src/components 2048-game/src/composables 2048-game/src/types
```

- [ ] **Step 2: Create package.json**

```json
{
  "name": "2048-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vue-tsc": "^2.0.0"
  }
}
```

- [ ] **Step 3: Create vite.config.ts**

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

- [ ] **Step 4: Create tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"]
}
```

- [ ] **Step 5: Create tsconfig.node.json**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 6: Create index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
    <title>2048</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🎮</text></svg>" />
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 7: Create src/vite-env.d.ts**

```typescript
/// <reference types="vite/client" />
declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

- [ ] **Step 8: Create src/main.ts**

```typescript
import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

createApp(App).mount('#app')
```

- [ ] **Step 9: Install dependencies**

```bash
cd 2048-game && npm install
```

Expected: `node_modules/` created, no errors, lockfile generated.

- [ ] **Step 10: Verify initial build**

```bash
npm run build
```

Expected: `dist/` folder created with index.html + JS bundle. No TypeScript errors.

- [ ] **Step 11: Commit**

```bash
git add 2048-game/
git commit -m "feat: scaffold Vite + Vue 3 + TypeScript 2048 project"
```

---

### Task 2: Types and Storage Layer

**Files:**
- Create: `2048-game/src/types/game.ts`
- Create: `2048-game/src/composables/useStorage.ts`

**Interfaces:**
- Produces: `TileData`, `BoardSize`, `Direction`, `GameStatus`, `SerializedTile`, `StorageData` type exports
- Produces: `useStorage()` returning `{ saveState, loadState, clearState, saveBestScore, loadBestScore, saveDarkMode, loadDarkMode }`

- [ ] **Step 1: Create src/types/game.ts**

```typescript
export interface TileData {
  id: number
  value: number
  row: number
  col: number
  isNew?: boolean
  isMerged?: boolean
}

export type BoardSize = 4 | 5 | 6
export type Direction = 'up' | 'down' | 'left' | 'right'
export type GameStatus = 'playing' | 'won' | 'lost'

export interface SerializedTile {
  id: number
  value: number
  row: number
  col: number
}

export interface StorageData {
  tiles: SerializedTile[]
  score: number
  boardSize: BoardSize
}

export interface GameState {
  tiles: TileData[]
  score: number
  bestScore: Record<BoardSize, number>
  status: GameStatus
  boardSize: BoardSize
  history: { tiles: TileData[]; score: number }[]
  isDarkMode: boolean
}
```

- [ ] **Step 2: Create src/composables/useStorage.ts**

```typescript
import type { BoardSize, GameState, StorageData } from '../types/game'

const STATE_KEY = '2048-game-state'
const BEST_KEY = '2048-best-score'
const DARK_KEY = '2048-dark-mode'

export function useStorage() {
  function isAvailable(): boolean {
    try {
      const k = '__test__'
      localStorage.setItem(k, '1')
      localStorage.removeItem(k)
      return true
    } catch {
      return false
    }
  }

  const available = isAvailable()

  function saveState(state: Pick<GameState, 'tiles' | 'score' | 'boardSize'>): void {
    if (!available) return
    try {
      const data: StorageData = {
        tiles: state.tiles.map(t => ({ id: t.id, value: t.value, row: t.row, col: t.col })),
        score: state.score,
        boardSize: state.boardSize,
      }
      localStorage.setItem(STATE_KEY, JSON.stringify(data))
    } catch { /* silent fallback */ }
  }

  function loadState(): StorageData | null {
    if (!available) return null
    try {
      const raw = localStorage.getItem(STATE_KEY)
      if (!raw) return null
      const parsed = JSON.parse(raw) as StorageData
      if (!parsed.tiles || typeof parsed.score !== 'number') {
        localStorage.removeItem(STATE_KEY)
        return null
      }
      return parsed
    } catch {
      localStorage.removeItem(STATE_KEY)
      return null
    }
  }

  function clearState(): void {
    if (!available) return
    try { localStorage.removeItem(STATE_KEY) } catch { /* silent */ }
  }

  function saveBestScore(size: BoardSize, score: number): void {
    if (!available) return
    try {
      const scores = loadBestScore()
      scores[size] = Math.max(scores[size] || 0, score)
      localStorage.setItem(BEST_KEY, JSON.stringify(scores))
    } catch { /* silent */ }
  }

  function loadBestScore(): Record<BoardSize, number> {
    if (!available) return { 4: 0, 5: 0, 6: 0 }
    try {
      const raw = localStorage.getItem(BEST_KEY)
      return raw ? { 4: 0, 5: 0, 6: 0, ...JSON.parse(raw) } : { 4: 0, 5: 0, 6: 0 }
    } catch {
      return { 4: 0, 5: 0, 6: 0 }
    }
  }

  function saveDarkMode(isDark: boolean): void {
    if (!available) return
    try { localStorage.setItem(DARK_KEY, JSON.stringify(isDark)) } catch { /* silent */ }
  }

  function loadDarkMode(): boolean | null {
    if (!available) return null
    try {
      const raw = localStorage.getItem(DARK_KEY)
      return raw ? JSON.parse(raw) : null
    } catch {
      return null
    }
  }

  return { saveState, loadState, clearState, saveBestScore, loadBestScore, saveDarkMode, loadDarkMode }
}
```

- [ ] **Step 3: Verify build**

```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 4: Commit**

```bash
git add 2048-game/src/types/ 2048-game/src/composables/
git commit -m "feat: add types and localStorage composable with privacy-mode fallback"
```

---

### Task 3: Core Game Logic Composable

**Files:**
- Create: `2048-game/src/composables/useGameLogic.ts`

**Interfaces:**
- Consumes: `TileData`, `BoardSize`, `Direction` from `types/game.ts`
- Produces: `useGameLogic()` returning `{ createInitialTiles, executeMove, generateTile, checkWin, checkLose, canMove, resetIdCounter }`

- [ ] **Step 1: Create src/composables/useGameLogic.ts**

```typescript
import type { TileData, BoardSize, Direction } from '../types/game'

interface SlideRowResult {
  result: (TileData | null)[]
  score: number
  moved: boolean
}

export function useGameLogic() {
  let nextTileId = 1

  function generateId(): number {
    return nextTileId++
  }

  function resetIdCounter(maxId?: number): void {
    nextTileId = (maxId || 0) + 1
  }

  function shuffle<T>(arr: T[]): T[] {
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]]
    }
    return arr
  }

  /**
   * Create initial 2-tile board at random positions.
   */
  function createInitialTiles(size: BoardSize): TileData[] {
    const positions: [number, number][] = []
    for (let r = 0; r < size; r++) {
      for (let c = 0; c < size; c++) {
        positions.push([r, c])
      }
    }
    shuffle(positions)

    const tiles: TileData[] = []
    for (let i = 0; i < 2; i++) {
      tiles.push({
        id: generateId(),
        value: Math.random() < 0.9 ? 2 : 4,
        row: positions[i][0],
        col: positions[i][1],
        isNew: true,
      })
    }
    return tiles
  }

  /**
   * Slide a single line toward index 0 ("left").
   * Merges adjacent equal values. The left tile survives, the right is consumed.
   */
  function slideRow(line: (TileData | null)[], size: number): SlideRowResult {
    const tiles = line.filter((t): t is TileData => t !== null)
    if (tiles.length === 0) {
      return { result: Array(size).fill(null), score: 0, moved: false }
    }

    const result: (TileData | null)[] = Array(size).fill(null)
    let score = 0
    let outputIdx = 0
    let i = 0

    while (i < tiles.length) {
      if (i + 1 < tiles.length && tiles[i].value === tiles[i + 1].value) {
        // Merge: keep first tile's id, consume the second
        result[outputIdx] = {
          id: tiles[i].id,
          value: tiles[i].value * 2,
          row: tiles[i].row,
          col: outputIdx,
          isMerged: true,
        }
        score += tiles[i].value * 2
        i += 2
      } else {
        result[outputIdx] = {
          ...tiles[i],
          col: outputIdx,
          isNew: false,
          isMerged: false,
        }
        i++
      }
      outputIdx++
    }

    // Detect movement by comparing original vs result
    let moved = false
    for (let j = 0; j < size; j++) {
      const orig = line[j]
      const next = result[j]
      if (orig === null && next !== null) moved = true
      else if (orig !== null && next === null) moved = true
      else if (orig !== null && next !== null && orig.id !== next.id) moved = true
    }

    return { result, score, moved }
  }

  /**
   * Translate (lineIndex, positionInLine) → (row, col) for each direction.
   * For 'left':  lineIdx=row, pos=col
   * For 'right': lineIdx=row, pos=(size-1-col)
   * For 'up':    lineIdx=col, pos=row
   * For 'down':  lineIdx=col, pos=(size-1-row)
   */
  function dirCoords(dir: Direction, lineIdx: number, pos: number, size: number): { r: number; c: number } {
    switch (dir) {
      case 'left':  return { r: lineIdx, c: pos }
      case 'right': return { r: lineIdx, c: size - 1 - pos }
      case 'up':    return { r: pos, c: lineIdx }
      case 'down':  return { r: size - 1 - pos, c: lineIdx }
    }
  }

  /**
   * Execute a full board move in the given direction.
   * Returns new tile positions and accumulated score.
   */
  function executeMove(tiles: TileData[], direction: Direction, size: BoardSize): {
    tiles: TileData[]
    score: number
    moved: boolean
  } {
    let totalScore = 0
    let anyMoved = false
    const newTiles: TileData[] = []

    for (let i = 0; i < size; i++) {
      // Extract line (row or column depending on direction)
      const line: (TileData | null)[] = Array(size).fill(null)
      for (let j = 0; j < size; j++) {
        const { r, c } = dirCoords(direction, i, j, size)
        const tile = tiles.find(t => t.row === r && t.col === c)
        line[j] = tile || null
      }

      // Slide the line toward index 0
      const { result, score, moved } = slideRow(line, size)
      totalScore += score
      if (moved) anyMoved = true

      // Map result back to board positions
      for (let j = 0; j < size; j++) {
        const tile = result[j]
        if (tile) {
          const { r, c } = dirCoords(direction, i, j, size)
          newTiles.push({ ...tile, row: r, col: c })
        }
      }
    }

    return { tiles: newTiles, score: totalScore, moved: anyMoved }
  }

  /**
   * Spawn a new random tile (90% = 2, 10% = 4) in an empty cell.
   */
  function generateTile(tiles: TileData[], size: BoardSize): TileData {
    const occupied = new Set(tiles.map(t => `${t.row},${t.col}`))
    const empty: [number, number][] = []
    for (let r = 0; r < size; r++) {
      for (let c = 0; c < size; c++) {
        if (!occupied.has(`${r},${c}`)) empty.push([r, c])
      }
    }
    const [row, col] = empty[Math.floor(Math.random() * empty.length)]
    return {
      id: generateId(),
      value: Math.random() < 0.9 ? 2 : 4,
      row,
      col,
      isNew: true,
    }
  }

  function checkWin(tiles: TileData[]): boolean {
    return tiles.some(t => t.value >= 2048)
  }

  function checkLose(tiles: TileData[], size: BoardSize): boolean {
    if (tiles.length < size * size) return false
    for (let r = 0; r < size; r++) {
      for (let c = 0; c < size; c++) {
        const tile = tiles.find(t => t.row === r && t.col === c)
        if (!tile) return false
        const right = tiles.find(t => t.row === r && t.col === c + 1)
        if (right && right.value === tile.value) return false
        const down = tiles.find(t => t.row === r + 1 && t.col === c)
        if (down && down.value === tile.value) return false
      }
    }
    return true
  }

  function canMove(tiles: TileData[], size: BoardSize): boolean {
    if (tiles.length < size * size) return true
    return !checkLose(tiles, size)
  }

  return {
    createInitialTiles,
    executeMove,
    generateTile,
    checkWin,
    checkLose,
    canMove,
    resetIdCounter,
    shuffle,
  }
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/composables/useGameLogic.ts
git commit -m "feat: add core 2048 game logic (slide, merge, win/lose detection)"
```

---

### Task 4: Game State Composable

**Files:**
- Create: `2048-game/src/composables/useGameState.ts`

**Interfaces:**
- Consumes: `GameState` from types; `useGameLogic()`; `useStorage()`
- Produces: `useGameState()` returning `{ state, initGame, restoreOrInit, handleMove, undo, toggleDarkMode, newGame }`

- [ ] **Step 1: Create src/composables/useGameState.ts**

```typescript
import { reactive } from 'vue'
import type { GameState, Direction } from '../types/game'
import { useGameLogic } from './useGameLogic'
import { useStorage } from './useStorage'

export function useGameState() {
  const logic = useGameLogic()
  const storage = useStorage()

  // Determine initial dark mode
  const savedDark = storage.loadDarkMode()
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
  const initialDark = savedDark !== null ? savedDark : prefersDark
  if (initialDark) document.documentElement.classList.add('dark')

  const state = reactive<GameState>({
    tiles: [],
    score: 0,
    bestScore: storage.loadBestScore(),
    status: 'playing',
    boardSize: 4,
    history: [],
    isDarkMode: initialDark,
  })

  let animationLocked = false

  function initGame(size: number = state.boardSize): void {
    const boardSize = size as 4 | 5 | 6
    state.tiles = logic.createInitialTiles(boardSize)
    state.score = 0
    state.boardSize = boardSize
    state.status = 'playing'
    state.history = []
    animationLocked = false
    storage.saveState({ tiles: state.tiles, score: state.score, boardSize: state.boardSize })
  }

  /**
   * Try restoring saved game from localStorage; start fresh if nothing saved.
   */
  function restoreOrInit(): void {
    const saved = storage.loadState()
    if (saved && saved.tiles.length > 0 && saved.boardSize) {
      state.tiles = saved.tiles.map(t => ({ ...t, isNew: false, isMerged: false }))
      state.score = saved.score
      state.boardSize = saved.boardSize
      state.status = 'playing'
      state.history = []
      state.bestScore = storage.loadBestScore()
    } else {
      initGame(4)
    }
  }

  function handleMove(direction: Direction): void {
    if (state.status !== 'playing') return
    if (animationLocked) return

    // Save snapshot for undo (deep clone tiles)
    state.history.push({
      tiles: state.tiles.map(t => ({ ...t, isNew: false, isMerged: false })),
      score: state.score,
    })
    if (state.history.length > 10) state.history.shift()

    const result = logic.executeMove(state.tiles, direction, state.boardSize)

    if (!result.moved) {
      state.history.pop() // undo wasted history entry
      return
    }

    state.score += result.score
    state.tiles = result.tiles

    // Spawn a new tile
    const spawnTile = logic.generateTile(state.tiles, state.boardSize)
    state.tiles.push(spawnTile)

    // Lock input during animation
    animationLocked = true
    setTimeout(() => { animationLocked = false }, 150)

    // Clear animation flags after animation completes
    setTimeout(() => {
      state.tiles.forEach(t => { t.isNew = false; t.isMerged = false })
    }, 250)

    // Auto-save
    storage.saveState({ tiles: state.tiles, score: state.score, boardSize: state.boardSize })

    // Update best score
    if (state.score > (state.bestScore[state.boardSize] || 0)) {
      state.bestScore[state.boardSize] = state.score
      storage.saveBestScore(state.boardSize, state.score)
    }

    // Check win / lose
    if (logic.checkWin(state.tiles)) {
      state.status = 'won'
    } else if (logic.checkLose(state.tiles, state.boardSize)) {
      state.status = 'lost'
    }
  }

  function undo(): void {
    if (state.history.length === 0) return
    const last = state.history.pop()!
    state.tiles = last.tiles.map(t => ({ ...t, isNew: false, isMerged: false }))
    state.score = last.score
    state.status = 'playing'
    storage.saveState({ tiles: state.tiles, score: state.score, boardSize: state.boardSize })
  }

  function toggleDarkMode(): void {
    state.isDarkMode = !state.isDarkMode
    document.documentElement.classList.toggle('dark', state.isDarkMode)
    storage.saveDarkMode(state.isDarkMode)
  }

  function newGame(size?: number): void {
    const s = size || state.boardSize
    if (s !== state.boardSize) {
      state.bestScore = storage.loadBestScore()
    }
    initGame(s)
  }

  return {
    state,
    initGame,
    restoreOrInit,
    handleMove,
    undo,
    toggleDarkMode,
    newGame,
  }
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/composables/useGameState.ts
git commit -m "feat: add game state composable with undo, auto-save, and restore"
```

---

### Task 5: Global CSS Styles

**Files:**
- Create: `2048-game/src/style.css`

- [ ] **Step 1: Create src/style.css**

```css
/* ========== CSS Variables & Theme ========== */
:root {
  --board-bg: #bbada0;
  --cell-bg: rgba(238, 228, 218, 0.35);
  --page-bg: #faf8ef;
  --text-color: #776e65;
  --score-bg: #bbada0;
  --btn-bg: #8f7a66;
  --btn-hover: #9f8b78;
  --overlay-bg: rgba(238, 228, 218, 0.73);
  --gap: 8px;
  --radius: 6px;
  --max-width: 500px;
}

.dark {
  --board-bg: #3c3a3d;
  --cell-bg: rgba(255, 255, 255, 0.1);
  --page-bg: #1a1a2e;
  --text-color: #e0e0e0;
  --score-bg: #3c3a3d;
  --btn-bg: #5c5a5d;
  --btn-hover: #6c6a6d;
  --overlay-bg: rgba(26, 26, 46, 0.85);
}

/* ========== Reset ========== */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background: var(--page-bg);
  color: var(--text-color);
  overflow-x: hidden;
  -webkit-tap-highlight-color: transparent;
  user-select: none;
}

#app {
  min-height: 100%;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding: 20px 16px 40px;
}

/* ========== Tile Colors ========== */
.tile-2    { background: #eee4da; color: #776e65; }
.tile-4    { background: #ede0c8; color: #776e65; }
.tile-8    { background: #f2b179; color: #f9f6f2; }
.tile-16   { background: #f59563; color: #f9f6f2; }
.tile-32   { background: #f67c5f; color: #f9f6f2; }
.tile-64   { background: #f65e3b; color: #f9f6f2; }
.tile-128  { background: #edcf72; color: #f9f6f2; font-size: 0.9em; }
.tile-256  { background: #edcc61; color: #f9f6f2; font-size: 0.9em; }
.tile-512  { background: #edc850; color: #f9f6f2; font-size: 0.9em; }
.tile-1024 { background: #edc53f; color: #f9f6f2; font-size: 0.75em; }
.tile-2048 { background: #edc22e; color: #f9f6f2; font-size: 0.75em; }
.tile-super { background: #3c3a32; color: #f9f6f2; font-size: 0.65em; }

.dark .tile-2    { background: #4a4a4a; color: #e0e0e0; }
.dark .tile-4    { background: #5a4a3a; color: #e0e0e0; }
.dark .tile-8    { background: #8a5a3a; color: #e0e0e0; }
.dark .tile-16   { background: #9a4a2a; color: #e0e0e0; }
.dark .tile-32   { background: #aa3a2a; color: #e0e0e0; }
.dark .tile-64   { background: #ba2a1a; color: #e0e0e0; }
.dark .tile-128  { background: #8a7a2a; color: #e0e0e0; }
.dark .tile-256  { background: #9a8a2a; color: #e0e0e0; }
.dark .tile-512  { background: #aa9a2a; color: #e0e0e0; }
.dark .tile-1024 { background: #baaa2a; color: #e0e0e0; }
.dark .tile-2048 { background: #ccaa1a; color: #e0e0e0; }
.dark .tile-super { background: #5a5a5a; color: #e0e0e0; }
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/style.css
git commit -m "feat: add global CSS with tile colors and dark mode theme"
```

---

### Task 6: GameTile Component

**Files:**
- Create: `2048-game/src/components/GameTile.vue`

**Interfaces:**
- Props: `tile: TileData`, `cellSize: number`, `gap: number`
- Pure display component — renders a positioned tile with pop-in and pulse animations

- [ ] **Step 1: Create src/components/GameTile.vue**

```vue
<template>
  <div
    class="tile"
    :class="[tileClass, { 'tile--new': tile.isNew, 'tile--merged': tile.isMerged }]"
    :style="tilePosition"
  >
    {{ tile.value }}
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { TileData } from '../types/game'

const props = defineProps<{
  tile: TileData
  cellSize: number
  gap: number
}>()

const tileClass = computed(() => {
  const v = props.tile.value
  if (v >= 8192) return 'tile-super'
  if (v <= 2048) return `tile-${v}`
  return 'tile-super'
})

const tilePosition = computed(() => ({
  transform: `translate(${props.tile.col * (props.cellSize + props.gap) + props.gap}px, ${props.tile.row * (props.cellSize + props.gap) + props.gap}px)`,
  width: `${props.cellSize}px`,
  height: `${props.cellSize}px`,
  lineHeight: `${props.cellSize}px`,
  fontSize: props.cellSize > 60
    ? (props.tile.value >= 1024 ? '1.6rem' : '2.5rem')
    : (props.tile.value >= 1024 ? '1.1rem' : '1.6rem'),
}))
</script>

<style scoped>
.tile {
  position: absolute;
  top: 0;
  left: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  font-weight: bold;
  border-radius: 4px;
  z-index: 10;
  transition: transform 100ms ease;
  will-change: transform;
  pointer-events: none;
}

.tile--new {
  animation: tile-pop-in 200ms ease-out;
}

.tile--merged {
  animation: tile-pulse 200ms ease-in-out;
}

@keyframes tile-pop-in {
  0%   { transform: scale(0); opacity: 0; }
  50%  { transform: scale(1.1); opacity: 1; }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes tile-pulse {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.15); }
  100% { transform: scale(1); }
}
</style>
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameTile.vue
git commit -m "feat: add GameTile with CSS transform translation and animations"
```

---

### Task 7: GameBoard Component

**Files:**
- Create: `2048-game/src/components/GameBoard.vue`

**Interfaces:**
- Props: `tiles: TileData[]`, `boardSize: BoardSize`
- Emits: `move(direction: Direction)`
- Renders CSS Grid for cell background + absolute-positioned tiles
- Handles keyboard arrow keys and touch swipe

- [ ] **Step 1: Create src/components/GameBoard.vue**

```vue
<template>
  <div
    ref="boardRef"
    class="board"
    :style="gridStyle"
    tabindex="0"
    role="grid"
    aria-label="2048 游戏棋盘"
    @keydown.prevent="onKeyDown"
    @touchstart.prevent="onTouchStart"
    @touchmove.prevent="onTouchMove"
    @touchend.prevent="onTouchEnd"
  >
    <!-- Background cells (CSS Grid) -->
    <div
      v-for="i in boardSize * boardSize"
      :key="'cell-' + i"
      class="cell"
    ></div>

    <!-- Tile layer (absolute positioned) -->
    <GameTile
      v-for="tile in tiles"
      :key="tile.id"
      :tile="tile"
      :cell-size="cellSize"
      :gap="gap"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onBeforeUnmount } from 'vue'
import type { BoardSize, Direction, TileData } from '../types/game'
import GameTile from './GameTile.vue'

const props = defineProps<{
  tiles: TileData[]
  boardSize: BoardSize
}>()

const emit = defineEmits<{
  move: [direction: Direction]
}>()

const gap = 8
const boardRef = ref<HTMLDivElement | null>(null)
const boardWidth = ref(500)

const cellSize = computed(() => {
  return (boardWidth.value - gap * (props.boardSize + 1)) / props.boardSize
})

const gridStyle = computed(() => ({
  width: `${boardWidth.value}px`,
  height: `${boardWidth.value}px`,
  gridTemplateColumns: `repeat(${props.boardSize}, ${cellSize.value}px)`,
  gridTemplateRows: `repeat(${props.boardSize}, ${cellSize.value}px)`,
  gap: `${gap}px`,
  padding: `${gap}px`,
}))

function updateBoardWidth() {
  if (boardRef.value?.parentElement) {
    const parentW = boardRef.value.parentElement.clientWidth
    const maxW = Math.min(parentW - 32, 500)
    boardWidth.value = Math.max(maxW, 280)
  }
}

// --- Keyboard ---
const keyMap: Record<string, Direction> = {
  ArrowUp: 'up', ArrowDown: 'down', ArrowLeft: 'left', ArrowRight: 'right',
}

function onKeyDown(e: KeyboardEvent) {
  const dir = keyMap[e.key]
  if (dir) {
    e.preventDefault()
    emit('move', dir)
  }
}

// --- Touch swipe ---
let touchStartX = 0
let touchStartY = 0
const MIN_SWIPE = 20

function onTouchStart(e: TouchEvent) {
  const t = e.touches[0]
  touchStartX = t.clientX
  touchStartY = t.clientY
}

function onTouchMove(_e: TouchEvent) {
  /* swallow to prevent scroll */
}

function onTouchEnd(e: TouchEvent) {
  const t = e.changedTouches[0]
  const dx = t.clientX - touchStartX
  const dy = t.clientY - touchStartY
  const absDx = Math.abs(dx)
  const absDy = Math.abs(dy)

  if (Math.max(absDx, absDy) < MIN_SWIPE) return

  if (absDx > absDy) {
    emit('move', dx > 0 ? 'right' : 'left')
  } else {
    emit('move', dy > 0 ? 'down' : 'up')
  }
}

// --- Resize observer ---
let resizeObserver: ResizeObserver | null = null

onMounted(() => {
  updateBoardWidth()
  boardRef.value?.focus()
  resizeObserver = new ResizeObserver(updateBoardWidth)
  if (boardRef.value?.parentElement) {
    resizeObserver.observe(boardRef.value.parentElement)
  }
})

onBeforeUnmount(() => {
  resizeObserver?.disconnect()
})
</script>

<style scoped>
.board {
  position: relative;
  background: var(--board-bg);
  border-radius: var(--radius);
  display: grid;
  outline: none;
  touch-action: none;
}

.cell {
  background: var(--cell-bg);
  border-radius: 4px;
}
</style>
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameBoard.vue
git commit -m "feat: add GameBoard with grid rendering, keyboard and touch input"
```

---

### Task 8: GameHeader Component

**Files:**
- Create: `2048-game/src/components/GameHeader.vue`

**Interfaces:**
- Props: `score: number`, `bestScore: number`
- Emits: `newGame`
- Displays title, current score, best score, and "新游戏" button

- [ ] **Step 1: Create src/components/GameHeader.vue**

```vue
<template>
  <div class="header">
    <div class="header-top">
      <h1 class="title">2048</h1>
      <div class="scores">
        <div class="score-box">
          <span class="score-label">分数</span>
          <span class="score-value" :key="score">{{ score }}</span>
        </div>
        <div class="score-box">
          <span class="score-label">最高分</span>
          <span class="score-value">{{ bestScore }}</span>
        </div>
      </div>
    </div>
    <div class="header-bottom">
      <p class="subtitle">合并数字，达到 <strong>2048</strong>！</p>
      <button class="btn" @click="$emit('newGame')">新游戏</button>
    </div>
  </div>
</template>

<script setup lang="ts">
defineProps<{
  score: number
  bestScore: number
}>()

defineEmits<{
  newGame: []
}>()
</script>

<style scoped>
.header {
  width: 100%;
  max-width: 500px;
  margin-bottom: 12px;
}

.header-top {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.title {
  font-size: 4rem;
  font-weight: 900;
  line-height: 1;
  color: var(--text-color);
}

.scores {
  display: flex;
  gap: 8px;
}

.score-box {
  background: var(--score-bg);
  color: #fff;
  border-radius: var(--radius);
  padding: 8px 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 80px;
}

.score-label {
  font-size: 0.7rem;
  text-transform: uppercase;
  opacity: 0.8;
  letter-spacing: 0.5px;
}

.score-value {
  font-size: 1.5rem;
  font-weight: 700;
}

.header-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 8px;
}

.subtitle {
  font-size: 0.9rem;
  color: var(--text-color);
  opacity: 0.8;
}

.btn {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 10px 20px;
  font-size: 0.9rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.15s;
  white-space: nowrap;
}

.btn:hover {
  background: var(--btn-hover);
}

@media (max-width: 500px) {
  .title { font-size: 3rem; }
  .score-box { padding: 6px 14px; min-width: 60px; }
  .score-value { font-size: 1.2rem; }
}
</style>
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameHeader.vue
git commit -m "feat: add GameHeader with score display and new game button"
```

---

### Task 9: GameControls Component

**Files:**
- Create: `2048-game/src/components/GameControls.vue`

**Interfaces:**
- Props: `canUndo: boolean`, `boardSize: BoardSize`, `isDark: boolean`
- Emits: `undo`, `changeSize: BoardSize`, `toggleDark`
- Provides undo button, board size selector, and dark mode toggle

- [ ] **Step 1: Create src/components/GameControls.vue**

```vue
<template>
  <div class="controls">
    <button class="btn" :disabled="!canUndo" @click="$emit('undo')">
      ↩ 撤销
    </button>

    <div class="control-group">
      <label class="control-label">棋盘</label>
      <select class="select" :value="boardSize" @change="onSizeChange">
        <option :value="4">4×4</option>
        <option :value="5">5×5</option>
        <option :value="6">6×6</option>
      </select>
    </div>

    <button class="btn btn-icon" @click="$emit('toggleDark')" :title="isDark ? '切换亮色模式' : '切换深色模式'">
      {{ isDark ? '☀️' : '🌙' }}
    </button>
  </div>
</template>

<script setup lang="ts">
import type { BoardSize } from '../types/game'

defineProps<{
  canUndo: boolean
  boardSize: BoardSize
  isDark: boolean
}>()

const emit = defineEmits<{
  undo: []
  changeSize: [size: BoardSize]
  toggleDark: []
}>()

function onSizeChange(e: Event) {
  const val = parseInt((e.target as HTMLSelectElement).value) as BoardSize
  emit('changeSize', val)
}
</script>

<style scoped>
.controls {
  width: 100%;
  max-width: 500px;
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 12px;
}

.btn {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 10px 16px;
  font-size: 0.85rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.15s;
  white-space: nowrap;
}

.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.btn:not(:disabled):hover {
  background: var(--btn-hover);
}

.btn-icon {
  padding: 10px 12px;
  font-size: 1.2rem;
  line-height: 1;
}

.control-group {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-left: auto;
}

.control-label {
  font-size: 0.8rem;
  color: var(--text-color);
  opacity: 0.8;
}

.select {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 9px 12px;
  font-size: 0.85rem;
  font-weight: 600;
  cursor: pointer;
  outline: none;
}

.select:hover {
  background: var(--btn-hover);
}

@media (max-width: 500px) {
  .controls { flex-wrap: wrap; }
  .control-group { margin-left: 0; }
}
</style>
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameControls.vue
git commit -m "feat: add GameControls with undo, board size selector, dark mode toggle"
```

---

### Task 10: GameOverlay Component

**Files:**
- Create: `2048-game/src/components/GameOverlay.vue`

**Interfaces:**
- Props: `status: GameStatus`, `score: number`
- Emits: `newGame`
- Win/lose overlay with semi-transparent backdrop, Vue `<Transition>` animation

- [ ] **Step 1: Create src/components/GameOverlay.vue**

```vue
<template>
  <Transition name="overlay">
    <div v-if="status !== 'playing'" class="overlay" @click.self="$emit('newGame')">
      <div class="overlay-content">
        <h2 class="overlay-title" v-if="status === 'won'">🎉 恭喜你赢了！</h2>
        <h2 class="overlay-title lose" v-else>😞 游戏结束</h2>
        <p class="overlay-score">得分：<strong>{{ score }}</strong></p>
        <button class="btn" @click="$emit('newGame')">再来一局</button>
      </div>
    </div>
  </Transition>
</template>

<script setup lang="ts">
import type { GameStatus } from '../types/game'

defineProps<{
  status: GameStatus
  score: number
}>()

defineEmits<{
  newGame: []
}>()
</script>

<style scoped>
.overlay {
  position: absolute;
  inset: 0;
  background: var(--overlay-bg);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 100;
  border-radius: var(--radius);
  backdrop-filter: blur(2px);
}

.overlay-content {
  text-align: center;
  padding: 32px;
}

.overlay-title {
  font-size: 2rem;
  margin-bottom: 12px;
  color: var(--text-color);
}

.overlay-score {
  font-size: 1.1rem;
  margin-bottom: 24px;
  color: var(--text-color);
}

.btn {
  background: var(--btn-bg);
  color: #f9f6f2;
  border: none;
  border-radius: var(--radius);
  padding: 12px 32px;
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.15s;
}

.btn:hover {
  background: var(--btn-hover);
}

/* Transition */
.overlay-enter-active {
  transition: opacity 0.3s ease;
}
.overlay-leave-active {
  transition: opacity 0.2s ease;
}
.overlay-enter-from,
.overlay-leave-to {
  opacity: 0;
}
</style>
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add 2048-game/src/components/GameOverlay.vue
git commit -m "feat: add GameOverlay for win/lose states with transition"
```

---

### Task 11: App.vue Final Integration

**Files:**
- Create: `2048-game/src/App.vue`

- [ ] **Step 1: Create src/App.vue**

```vue
<template>
  <div class="app-container">
    <GameHeader
      :score="state.score"
      :best-score="state.bestScore[state.boardSize] || 0"
      @new-game="handleNewGame"
    />

    <div class="board-wrapper">
      <GameBoard
        :tiles="state.tiles"
        :board-size="state.boardSize"
        @move="handleMove"
      />
      <GameOverlay
        :status="state.status"
        :score="state.score"
        @new-game="handleNewGame"
      />
    </div>

    <GameControls
      :can-undo="state.history.length > 0"
      :board-size="state.boardSize"
      :is-dark="state.isDarkMode"
      @undo="state.undo()"
      @change-size="handleSizeChange"
      @toggle-dark="state.toggleDarkMode()"
    />
  </div>
</template>

<script setup lang="ts">
import { onMounted } from 'vue'
import GameHeader from './components/GameHeader.vue'
import GameBoard from './components/GameBoard.vue'
import GameControls from './components/GameControls.vue'
import GameOverlay from './components/GameOverlay.vue'
import { useGameState } from './composables/useGameState'
import type { BoardSize, Direction } from './types/game'

const { state, handleMove, restoreOrInit, newGame } = useGameState()

function handleNewGame(): void {
  newGame(state.boardSize)
}

function handleSizeChange(size: BoardSize): void {
  newGame(size)
}

onMounted(() => {
  restoreOrInit()
})
</script>

<style scoped>
.app-container {
  width: 100%;
  max-width: var(--max-width, 500px);
  display: flex;
  flex-direction: column;
  align-items: center;
}

.board-wrapper {
  position: relative;
  width: 100%;
  display: flex;
  justify-content: center;
}
</style>
```

- [ ] **Step 2: Delete stub App.vue if it exists and create the new one**

```bash
rm 2048-game/src/App.vue 2>/dev/null
```

Then write the new App.vue content shown in Step 1.

- [ ] **Step 3: Final build**

```bash
npm run build
```

Expected: build succeeds. No TypeScript errors. Output:

```
dist/index.html
dist/assets/index-XXXX.js
dist/assets/index-XXXX.css
```

- [ ] **Step 4: Commit**

```bash
git add 2048-game/src/App.vue
git commit -m "feat: integrate all components in App.vue with game state"
```

---

### Task 12: Verify Game Works

**Files:**
- None (play/test the app)

- [ ] **Step 1: Start dev server**

```bash
npm run dev
```

Expected: Vite dev server starts at `http://localhost:5173`.

- [ ] **Step 2: Manual test checklist**

Open the browser and verify:

1. **Board renders** — 4×4 grid with 2 random tiles at game start
2. **Keyboard controls** — Arrow keys move tiles, matching numbers merge
3. **Touch controls** — Swipe on mobile triggers movement
4. **Score updates** — Merging tiles increases score
5. **Best score persists** — Refresh page, best score stays
6. **Game state restores** — Mid-game refresh restores board and score
7. **Undo** — Undo button reverts last move (up to 10 steps)
8. **Board size** — Switch to 5×5 or 6×6, game resets with new size
9. **Dark mode** — Toggle dark mode, colors switch correctly
10. **Win overlay** — Reach 2048, overlay appears with "恭喜你赢了！"
11. **Lose overlay** — Fill board with no moves possible, overlay with "游戏结束"
12. **New game** — Click "新游戏" or overlay button, board resets
13. **Responsive** — Resize browser below 500px, board scales down

- [ ] **Step 3: Fix any issues found**

If any test fails, fix the root cause and re-run `npm run build`.

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "fix: resolve issues from manual testing"
```
