server:
  port: 8080

spring:
  application:
    name: stock-platform-java

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/stock_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-Flex 配置
mybatis-flex:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  base-package: com.stockplatform.mapper