[project]
name = "core"
version = "0.1.0"
description = "公共底层工具包 — 被所有其他子项目依赖"
requires-python = ">=3.10"
packages = [{ include = "core", from = "src" }]

dependencies = ["requests>=2.31,<3"]

[build-system]
requires = ["uv_build>=0.11.0,<0.12"]
build-backend = "uv_build"
