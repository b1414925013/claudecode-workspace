[project]
name = "uv-mono-demo"
version = "0.1.0"
description = "uv workspace 演示项目 — 多子项目 + 跨包调用"
requires-python = ">=3.10"

[tool.uv.workspace]
members = [
  "core",
  "service_a",
  "service_b",
  "cli-tool",
]
